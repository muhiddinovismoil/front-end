import LoadingSpinner from "@/components/loading/loading";
import React from "react";

const Loading = () => {
    return (
        <>
            <LoadingSpinner />
        </>
    );
};

export default Loading;
